#ifndef UTILITY_MODE_HANDLING_H_   /* Include guard */
#define UTILITY_MODE_HANDLING_H_

// Used to check if a not default mode is wanted
void check_startup_mode(void);

void select_mode(void);

void other_call(void);

#endif
